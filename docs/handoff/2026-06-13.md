# Handoff — 2026-06-13

## What just happened

The user ran `/grill-with-docs` against a fresh Frontend Mentor space-tourism starter and walked through a 17-question decision session. Settled decisions were captured in 7 ADRs, a `decisions-2026-06.md` log, and a runtime-flow walkthrough. Then the user asked to convert the plan into a PRD and break it into issues. The full skill stack was set up: `/setup-matt-pocock-skills` (GitHub Issues, default triage labels, single-context docs), `/to-prd` (PRD published as GitHub issue #1), and `/to-issues` (6 vertical slices published as issues #2–#7). Working tree committed at `adba97e` and pushed to `https://github.com/bedo22/space-tourism-website-main`.

**Next session focus (recommended):** start implementation via `/tdd` on the first slice (GitHub issue #2 — Repo skeleton + test harness + CUBE layer files). All architectural decisions are settled; the build can begin.

## State of the world

- **Repo:** `E:\Freelancer\Frontend Mentor\space-tourism-website-main` (cloned from `bedo22/space-tourism-website-main` on GitHub)
- **Branch:** `main` at commit `adba97e`
- **Remote:** configured and authenticated; `gh auth status` shows `repo` scope
- **Working tree:** clean (everything is committed and pushed)
- **Triage labels:** `ready-for-agent` is created on the repo. The other four (`needs-triage`, `needs-info`, `ready-for-human`, `wontfix`) can be created on first use or via the one-shot `gh label create` block in `docs/agents/triage-labels.md`

## Issue tracker (current state)

| # | Title | Label | Blocked by |
|---|---|---|---|
| 1 | PRD: Space Tourism website v1 | (none) | — |
| 2 | Slice 1: Repo skeleton + test harness + CUBE layer files | `ready-for-agent` | — |
| 3 | Slice 2: Static HTML for all 4 pages + WOFF2 + CUBE CSS | `ready-for-agent` | #2 |
| 4 | Slice 3: `data.json` loader + responsive image source helper | `ready-for-agent` | #2 |
| 5 | Slice 4: Generic Tab component (factory + ARIA + keyboard + reduced-motion fade) | `ready-for-agent` | #2, #3, #4 |
| 6 | Slice 5: Per-page wire-up + hash contract + image pre-decode + deep-link focus | `ready-for-agent` | #4, #5 |
| 7 | Slice 6: Mobile nav overlay + focus trap + error panel on data failure | `ready-for-agent` | #3, #5 |

`gh issue list --label ready-for-agent --state open` will show all six slices.

## Key artifacts (do not duplicate — reference by path)

- **PRD (full):** `docs/prd/space-tourism-v1.md` — 59 user stories, implementation decisions, testing decisions, out-of-scope
- **Grill decisions log:** `docs/architecture/decisions-2026-06.md` — Q1–Q20 settled, Q21–Q26 deferred
- **Runtime walkthrough:** `docs/architecture/runtime-flow.md` (English) and `runtime-flow.ar.md` (paired Arabic)
- **ADRs:** `docs/adr/0001` through `docs/adr/0007` (data layer + image-loading addendum, no build step, self-hosted fonts, two-breakpoint responsive, URL hash + focus addendum, ARIA Tabs keyboard contract, responsive images)
- **Glossary:** `CONTEXT.md` (Item, Tabstrip, Tab, Tab panel, Crew, Crew Role, Destination, Distance, Travel time, Technology)
- **Skill config:** `CLAUDE.md` + `docs/agents/{issue-tracker,triage-labels,domain}.md`
- **Slice bodies:** `.scratch/slice-1.md` through `.scratch/slice-6.md` (full text, used to publish issues)
- **Design reference:** `starter-code/Design/` PNGs (per-page per-breakpoint per-item)
- **Data:** `starter-code/data.json`

## Key user preferences to remember

- **Glossary discipline:** domain terms go in `CONTEXT.md`; hard-to-reverse + surprising + trade-off decisions go in `docs/adr/`; everything else lives in code. Source: the team's `feedback-contContext-adr-implementation-split.md` memory.
- **Complexity pushback:** when a recommendation introduces non-trivial complexity (dup data source, build step, pre-render, single-caller abstraction), include a "case for not doing this" paragraph in the same response. Trigger phrases: "is it really necessary", "is this overdoing", "do you feel". Source: `feedback-complexity-pushback.md`.
- **Concept explanations:** user wants web-platform AND JS-architecture concepts (cache, LCP, slugify, factory function, render function, etc.) spelled out before recommendations. Source: `user-learning-posture-grilling.md`.
- **Bilingual artifacts:** Arabic / non-English versions of project docs do NOT go in the project repo by default. The runtime-flow Arabic translation was created as a paired `.ar.md` next to the English original; the user opted into that exception explicitly. Source: `feedback-bilingual-artifact-routing.md`.
- **CUBE layer file naming:** `composition.css` / `block.css` / `utility.css` / `exception.css` (no underscore prefix; one file per CUBE layer; the user picked this style during /setup-matt-pocock-skills).

## Suggested next skills for the next session

1. **`/tdd` on issue #2 (Slice 1: Repo skeleton).** Slice 1 is foundational — adds `package.json` with vitest+jsdom, the four CUBE CSS files as placeholders, the `js/` module directory, and one smoke test. No user-visible behavior. Smallest end-to-end landing. Once #2 lands, #3 and #4 can run in parallel.
2. **`/grill-with-docs`** only if a new ambiguous decision surfaces during implementation. The Q21–Q26 deferred list is small and most are answerable in-context; reopen the grill only if the answer isn't obvious.
3. **`/triage`** on incoming bug reports during implementation. Not yet needed.
4. **`/qa`** if the user reports an issue. Not yet needed.
5. **`/to-prd`** is not needed again — the PRD is at issue #1 and `docs/prd/space-tourism-v1.md`.
6. **`/improve-codebase-architecture`** after slice 7 lands. Will be useful to spot refactor opportunities once the implementation has settled.

## Open context worth flagging

- The first end-to-end test seam (load `crew.html`, assert first Tab active, panel shows first item) lives in slice 5. Slice 1–4 each have their own test boundaries. The user explicitly preferred the **highest seam possible** (per the PRD's testing decisions).
- `prefers-reduced-motion: reduce` is referenced in slices 4 and 6 — make sure the test harness can simulate the media query (vitest with jsdom supports this via `matchMedia` mock).
- The Home page kicker is **absent** (per design PNGs). Don't add one. This is a deliberate decision from the grill and could easily be "fixed" by an agent reading only the PRD's "no kicker" wording and missing the design.

## Things explicitly NOT to do

- Do not start a build step (no Vite, no PostCSS, no bundler — ADR-0002).
- Do not re-export Technology images to WebP (Q20 user decision — "practice project, things have gotten too long").
- Do not add 3x-density images (Q20).
- Do not add a focus-trap library — hand-roll the ~10 lines in `js/mobile-nav.js`.
- Do not write pre-JS HTML for Tab panel content (Q12 user pushback — keep Tabstrip static only).
- Do not close or modify issue #1 (the parent PRD). The skill explicitly says not to.
