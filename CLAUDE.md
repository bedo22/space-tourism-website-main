# CLAUDE.md

## Agent skills

### Issue tracker

GitHub Issues (this repo: `bedo22/space-tourism-website-main`). Skills call the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-state vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` at repo root + `docs/adr/` for ADRs. See `docs/agents/domain.md`.
